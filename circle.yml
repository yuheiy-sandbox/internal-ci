machine:
  timezone: Asia/Tokyo
  node:
    version: 6

dependencies:
  cache_directories:
    - node_modules

deployment:
  staging:
    branch: development
    commands:
      - ./scripts/deploy-to-staging.sh
