machine:
  timezone: Asia/Tokyo
  node:
    version: 6

dependencies:
  # pre:
  #   - npm install
  cache_directories:
    - node_modules

test:
  override:
    - npm run build

deployment:
  staging:
    branch: development
    commands:
      - ./scripts/deploy-to-staging.sh
