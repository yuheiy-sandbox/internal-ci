machine:
  timezone: Asia/Tokyo
  node:
    version: 6

dependencies:
  cache_directories:
    - node_modules

test:
  override:
    - npm run build

deployment:
  staging:
    branch: development
    commands:
      - bash ./scripts/deploy-to-staging.sh
